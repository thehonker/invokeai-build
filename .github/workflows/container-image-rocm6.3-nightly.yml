name: InvokeAI Docker Image Build - ROCM 6.3 - Nightly
on:
  push:
    branches:
      - main
  schedule:
    # 00:00 on Sunday
    - cron: "0 0 * * *"
  workflow_dispatch: {}

concurrency:
  group: "invokeai-nightly-rocm63"
  cancel-in-progress: false

jobs:
  build-and-push:
    uses: ./.github/workflows/container-image-ci-base.yml
    with:
      gpu-driver: rocm6.3
      build-type: hash
